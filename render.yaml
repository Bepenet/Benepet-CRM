services:
  - type: web
    name: benepet-crm
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: dados-petcrm
      mountPath: /data
      sizeGB: 1